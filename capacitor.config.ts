import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.sentinel.idverifier',
  appName: 'Sentinel ID Verifier',
  webDir: 'dist'
};

export default config;
